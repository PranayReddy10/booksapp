<?xml version="1.0" encoding="utf-8"?>
<!-- Status bar behind the home screen: the same warm tint the page gradient starts
     with, so the system header reads as part of the app rather than a white strip. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/home_grad_top" />
</shape>
