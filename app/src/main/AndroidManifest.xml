<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Task reminders.
         SCHEDULE_EXACT_ALARM (not USE_EXACT_ALARM) is deliberate: USE_EXACT_ALARM is
         auto-granted but Play restricts it to alarm/clock/calendar apps, so asking the
         user for it keeps the release reviewable. Reminders degrade to inexact when it
         is refused rather than failing outright. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Let the WebView / Razorpay detect installed UPI & payment apps (Android 11+
         package visibility). Without this, UPI options are hidden in the WebView. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="upi" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <package android:name="com.google.android.apps.nbu.paisa.user" /> <!-- Google Pay -->
        <package android:name="com.phonepe.app" />                        <!-- PhonePe -->
        <package android:name="net.one97.paytm" />                        <!-- Paytm -->
        <package android:name="in.org.npci.upiapp" />                     <!-- BHIM -->
        <package android:name="com.whatsapp" />                           <!-- WhatsApp (UPI + chat) -->
        <package android:name="com.amazon.mShop.android.shopping" />      <!-- Amazon Pay -->
    </queries>

    <application
        android:name="com.jntuh.util.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidEBook_V16"
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic,android:theme">
        <activity
            android:name="com.jntuh.books.SplashActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/RemoveAppSplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="@string/host"
                    android:pathPrefix="@string/pathPrefix"
                    android:scheme="@string/scheme" />
                <data
                    android:host="@string/get_host"
                    android:pathPrefix="@string/get_pathPrefix"
                    android:scheme="@string/get_scheme" />

            </intent-filter>

            <!-- App Links for the public website (posts + books). autoVerify uses
                 the assetlinks.json served at read.jntubooks.in/.well-known/. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" android:host="read.jntubooks.in" android:pathPrefix="/post/" />
                <data android:scheme="https" android:host="read.jntubooks.in" android:pathPrefix="/book/" />
            </intent-filter>
        </activity>
        <activity android:name="com.jntuh.books.IntroActivity" />
        <activity android:name="com.jntuh.books.LoginActivity" />
        <activity
            android:name="com.jntuh.books.MainActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name="com.jntuh.books.VideoPlayerActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="fullSensor" />
        <activity android:name="com.jntuh.books.RegisterActivity" />
        <activity android:name="com.jntuh.books.CompleteProfileActivity" />
        <activity android:name="com.jntuh.books.ForgotPassActivity" />
        <activity android:name="com.jntuh.books.SubCategoryActivity" />
        <activity android:name="com.jntuh.books.BookListBySubCatActivity" />
        <activity android:name="com.jntuh.books.BookDetailsActivity" />
        <activity android:name="com.jntuh.books.RateReviewActivity" />
        <activity android:name="com.jntuh.books.SettingsActivity" />
        <activity android:name="com.jntuh.books.AboutUsActivity" />
        <activity android:name="com.jntuh.books.PagesActivity" />
        <activity
            android:name="com.jntuh.books.PDFShow"
            android:configChanges="orientation|screenLayout|screenSize" />
        <activity
            android:name="com.jntuh.books.EditProfileActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name="com.jntuh.books.SearchBookActivity" />
        <activity android:name="com.jntuh.books.PlanListActivity" />
        <activity android:name="com.jntuh.books.PaymentMethodActivity" />
        <activity android:name="com.jntuh.books.HomeSectionListActivity" />
        <activity android:name="com.jntuh.books.FilterSearchBookActivity" />
        <activity android:name="com.jntuh.books.AuthorDetailsActivity" />
        <activity android:name="com.jntuh.books.TrendingBookActivity" />
        <activity android:name="com.jntuh.books.DownloadActivity" />
        <activity android:name="com.jntuh.books.UploadBookActivity" />
        <activity android:name="com.jntuh.books.MyUploadsActivity" />
        <activity android:name="com.jntuh.books.ProfileSectionActivity" />
        <activity android:name="com.jntuh.books.MediaFeedActivity" />
        <activity android:name="com.jntuh.books.MediaUploadActivity" />
        <activity android:name="com.jntuh.books.ShopProductDetailActivity" />
        <activity android:name="com.jntuh.books.ShopWebActivity" />
        <activity android:name="com.jntuh.books.MyMediaActivity" />
        <activity android:name="com.jntuh.books.MediaCommentsActivity" />
        <activity android:name="com.jntuh.books.MediaNotificationActivity" />
        <activity android:name="com.jntuh.paymentmethod.PayStackActivity" />
        <activity android:name="com.jntuh.paymentmethod.StripeActivity" />
        <activity android:name="com.jntuh.paymentmethod.PayPalActivity" />
        <activity android:name="com.jntuh.paymentmethod.RazorPayActivity" />
        <activity android:name="com.jntuh.paymentmethod.PayUMoneyActivity" />
        <activity android:name="com.jntuh.paymentmethod.FlutterWaveActivity" />
        <activity android:name="com.jntuh.paymentmethod.MyCinetPayActivity" />
        <activity android:name="com.jntuh.paymentmethod.ManualBankPaymentActivity" />
        <activity android:name="com.jntuh.paymentmethod.SslCommerzActivity" />
        <activity
            android:name="com.folioreader.ui.activity.FolioActivity"
            android:label="@string/app_name"
            android:theme="@style/FolioActivityDayTheme" />

        <meta-data
            android:name="applovin.sdk.key"
            android:value="@string/applovin_sdk_key" />
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_id" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name="com.google.android.gms.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize" />

        <meta-data
            android:name="com.wortise.ads.utm"
            android:value="viaviweb" />
        <meta-data
            android:name="firebase_crash_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/app_name" />

        <activity
            android:name="com.folioreader.ui.activity.SearchActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <receiver
            android:name="com.jntuh.service.DownloadCancelReceiver"
            android:exported="false" />

        <receiver
            android:name="com.razorpay.RzpTokenReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="rzp.device_token.share" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.razorpay.CheckoutActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:exported="true"
            android:theme="@style/CheckoutTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <data
                    android:host="rzp.io"
                    android:scheme="io.rzp" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.onesignal.NotificationOpened.DEFAULT"
            android:value="DISABLE" />

        <!-- My Results feature -->
        <activity android:name="com.jntuh.books.EditResultActivity" />
        <activity android:name="com.jntuh.books.ReportCardActivity" />

        <!-- Tasks & reminders -->
        <activity
            android:name="com.jntuh.books.TasksActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name="com.jntuh.books.AddEditTaskActivity"
            android:windowSoftInputMode="adjustResize" />

        <!-- The alarm takeover. singleInstance so a second alarm replaces the screen
             instead of stacking, and excluded from recents so a dismissed alarm cannot
             be resurrected from the task switcher. -->
        <activity
            android:name="com.jntuh.books.AlarmRingActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleInstance"
            android:showWhenLocked="true"
            android:taskAffinity=""
            android:theme="@style/Theme.AlarmRing"
            android:turnScreenOn="true" />

        <receiver
            android:name="com.jntuh.service.AlarmReceiver"
            android:exported="false" />

        <receiver
            android:name="com.jntuh.service.TaskActionReceiver"
            android:exported="false" />

        <!-- Alarms do not survive any of these, so each one rebuilds the schedule. -->
        <receiver
            android:name="com.jntuh.service.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.jntuh.service.AlarmSoundService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Used to share the generated report card PNG as an image file. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>